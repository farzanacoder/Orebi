import React from 'react'

const Pagi_head = ({text,className}) => {
  return (
    <h4 className={`font-bold font-dm pb-5 text-base text-secondary border-b border-[#F0F0F0] ${className}`}>{text}</h4>
  )
}

export default Pagi_head